---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.8.0
  creationTimestamp: null
  name: ceoslabdevices.ceoslab.arista.com
spec:
  group: ceoslab.arista.com
  names:
    kind: CEosLabDevice
    listKind: CEosLabDeviceList
    plural: ceoslabdevices
    singular: ceoslabdevice
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CEosLabDevice is the Schema for the ceoslabdevices API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: CEosLabDeviceSpec defines the desired state of CEosLabDevice
            properties:
              args:
                description: Additional arguments to pass to /sbin/init.  Those necessary
                  to boot properly are already present.
                items:
                  type: string
                type: array
              envvars:
                additionalProperties:
                  type: string
                description: Additional environment variables. Those necessary to
                  boot properly are already present.
                type: object
              image:
                description: 'Image name. Default: ceos:latest'
                type: string
              numinterfaces:
                description: Number of data interfaces to create. An additional interface
                  (eth0) is created for pod connectivity.
                format: int32
                type: integer
              resourcerequirements.omitempty:
                additionalProperties:
                  type: string
                description: Resource requirements/constraints
                type: object
              services.omitempty:
                additionalProperties:
                  type: string
                description: 'Port mappings for container services. Default: ssh="22:TCP",
                  ssl="443:TCP", gnmi="6030:TCP". Syntax: <port>:<TCP|UDP|TCP,UDP>'
                type: object
              sleep:
                description: 'Sleep time before starting ceoslab device. Default:
                  0'
                format: int32
                type: integer
            required:
            - resourcerequirements.omitempty
            - services.omitempty
            type: object
          status:
            description: CEosLabDeviceStatus defines the observed state of CEosLabDevice
            properties:
              reason:
                description: Reason for potential failure
                type: string
              status:
                description: Device status
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
